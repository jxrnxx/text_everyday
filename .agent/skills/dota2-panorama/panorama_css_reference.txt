align
面板对齐方式，第一个参数填水平对齐方式，第二个参数填垂直对齐方式

属性值
值   描述
top 上对齐
bottom  下对齐
left    左对齐
right   右对齐
center  居中对齐
范例
align: right center;    // 水平居右，垂直居中
horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
vertical-align
垂直方向上的定位

属性值
值   描述
top 默认，顶部
bottom  底部
center  垂直居中
middle  等同于center
范例
vertical-align: bottom;

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-direction
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-iteration-count
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation-timing-function
<Needs a description>

animation-delay
<Needs a description>

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-direction
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-iteration-count
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation-timing-function
<Needs a description>

transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;

animation-direction
<Needs a description>

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-iteration-count
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation-timing-function
<Needs a description>

animation-duration
定义一个动画播放的时间

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-direction
<Needs a description>

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-iteration-count
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation-timing-function
<Needs a description>

transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-direction
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-iteration-count
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation-timing-function
<Needs a description>

animation-iteration-count
<Needs a description>

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-direction
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation-timing-function
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-direction
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-iteration-count
<Needs a description>

animation-timing-function
<Needs a description>

animation-timing-function
<Needs a description>

animation
用于定义动画的属性

范例
animation-name: Gleam;
animation-duration: 3s;
animation-delay: 2.5s;
animation-iteration-count: infinite;
animation-direction: normal;
//透明度变化动画
@keyframes Gleam {
    0% {opacity: 0.1;}
    1.5% {opacity: 0.2;}
    10% {opacity: 0.3;}
    20% {opacity: 1;}
    100% {opacity: 1;}
}
animation-delay
<Needs a description>

animation-direction
<Needs a description>

animation-duration
定义一个动画播放的时间

animation-fill-mode
animation-fill-mode用于定义动画播放前和结束后的显示样式.

属性值
值   描述
none    默认值。结束后立即返回到初始状态
forwards    画结束后，元素将保留动画最后一帧的样式（即动画结束时的样式）
backwards   在动画播放之前（即在动画开始之前），元素将应用动画的第一帧的样式。
both    结合了 forwards 和 backwards，元素在动画开始之前将应用第一帧的样式，并在动画结束后保留最后一帧的样式
animation-iteration-count
<Needs a description>

animation-name
animation-name 属性为 @keyframes 动画规定名称。请始终规定 animation-duration 属性，否则时长为 0，就不会播放动画了。

transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );

background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
blur
Sets the amount of blur to apply to the panel and all it's children during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
blur: gaussian( 2.5 );
blur: gaussian( 6, 6, 1 );
world-blur
Sets the amount of blur to apply to the world / backbuffer before drawing.

范例
world-blur: gaussian( 2.5 );
world-blur: gaussian( 6, 6, 1 );
world-blur: mipmapgaussian( 6, 6, 4 );  In this version each gaussian pass is preceded by a quarter area downsample.

background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
opacity-mask-scale

background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px

background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
img-shadow
Specifies image shadows. The shadow shape will match the image the panel can generate,and this is only meaningful for images. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
img-shadow: 2px 2px 8px 3.0 #333333b0;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
opacity-mask-scale

background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;

background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px

background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;

background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-image
在背景中绘制图像或视频的逗号分隔列表。可以指定“none”不绘制背景层。结合background-position, background-size和background-repeat值。

范例
background-image: url("file://{images}/default.tga"), url( "file://{movies}/Background1080p.webm" );
background-image: url("s2r://panorama/images/challenges/icon_challenges_denies_png.vtex");
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
background-repeat
Controls if the background should be repeated in the horizontal and vertical directions. Possible values per direction: "repeat" - (default) Repeated in the specified direction until it fills the panel "space" - Repeated as many times as required to fill the panel w/o being clipped. Space is added between images to to align first and last image with panel edges. "round" - Repeated as many times as required to fill the panel w/o being clipped. The image is resized to align first and last image with panel edges. "no-repeat" - Not repeated

Possible single values: "repeat-x" - equals "repeat no-repeat" "repeat-y" - equals "no-repeat repeat"

范例
background-repeat: repeat; // equals "repeat repeat" (default)
background-repeat: repeat space; // repeats horizontally, spaces vertically 
background-repeat: no-repeat round; // 1 column of images, scaled to fit evenly
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;

blur
Sets the amount of blur to apply to the panel and all it's children during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
blur: gaussian( 2.5 );
blur: gaussian( 6, 6, 1 );
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
world-blur
Sets the amount of blur to apply to the world / backbuffer before drawing.

范例
world-blur: gaussian( 2.5 );
world-blur: gaussian( 6, 6, 1 );
world-blur: mipmapgaussian( 6, 6, 4 );  In this version each gaussian pass is preceded by a quarter area downsample.

border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;

border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
padding-bottom
<Needs a description>

border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
padding-bottom
<Needs a description>

background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
padding-bottom
<Needs a description>

margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
padding-left
<Needs a description>

border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
padding-bottom
<Needs a description>

brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
padding-right
<Needs a description>

border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
padding-bottom
<Needs a description>

font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;

border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
padding-bottom
<Needs a description>

max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );

border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
padding-left
<Needs a description>

border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
padding-left
<Needs a description>

background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
padding-left
<Needs a description>

font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;

border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
padding-left
<Needs a description>

max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;

border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
padding-right
<Needs a description>

border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
padding-right
<Needs a description>

background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
padding-right
<Needs a description>

font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;

border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
padding-right
<Needs a description>

max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;

border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
padding-top
<Needs a description>

border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
padding-top
<Needs a description>

background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
padding-top
<Needs a description>

margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
padding-left
<Needs a description>

border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
padding-top
<Needs a description>

brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
padding-right
<Needs a description>

border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
padding-top
<Needs a description>

font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;

border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
padding-top
<Needs a description>

max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
border
Shorthand for setting panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border: 2px solid #111111FF;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-radius
Shorthand to set border radius for all corners at once. Border radius rounds off corners of the panel, adjusting the border to smoothly round and also clipping background image/color and contents to the specified elliptical or circular values. In this shorthand version you may specify a single value for all raddi, or horizontal / vertical separated by the '/' character. For both horizontal and vertical you may specify 1 to 4 values in pixels or %, they will be taken in order as top-left, top-right, bottom-right, bottom-left radii values.

范例
// 2 px circular corners on all sides
border-radius: 2px;
// Perfect circular or elliptical panel (circular if box was square)
border-radius: 50% / 50%;
// 2 px horizontal radii 4px vertical elliptical corners on all sides
border-radius: 2px / 4px;
// All corners fully specified 
border-radius: 2px 3px 4px 2px / 2px 3px 3px 2px;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

box-shadow
Specifies shadows for boxes, or inset shadows/glows. The shadow shape will match the border box for the panel,so use border-radius to affect rounding. Syntax takes optional shape 'inset', 'fill', or 'hollow', then color, horizontal offset pixels, vertical offset pixels, blur radius pixels, and spread distance in pixels. Inset means the shadow is an inner shadow/glow, fill is a a shadow behind the entire box, hollow means clipping it to outside the border area only (before offset). A negative blur radius will give a hard-edged look to the shadow, effectively a rounded outline of the same size as the blur.

范例
box-shadow: #ffffff80 4px 4px 8px 0px; // outer, filled
box-shadow: hollow #ffffff80 4px 4px 8px 0px; // outer, hollow
box-shadow: inset #333333b0 0px 0px 8px 12px; // inner
img-shadow
Specifies image shadows. The shadow shape will match the image the panel can generate,and this is only meaningful for images. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
img-shadow: 2px 2px 8px 3.0 #333333b0;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;

brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;

clip
Specifies a clip region within the panel, where contents will be clipped at render time. This clipping has no impact on layout, and is fast and supported for transitions/animations. Radial clip mode takes a center point, start angle and angular width of the revealed sector.

范例
clip: rect( 10%, 90%, 90%, 10% );clip: radial( 50% 50%, 0deg, 90deg );

color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;

context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;

context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;

context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;


contrast
在合成过程中设置应用于面板及其所有子面板的对比度。

范例
contrast: 1.5;

flow-children
子元素的排版方式

属性值
值   描述
none    默认，无
right   水平排版
down    垂直排版
right-wrap  水平排版，超过父类边界的子元素自动换行
down-wrap   垂直排版，超过父类边界的子元素自动换行
范例
flow-children: down;
flow-children: right;
flow-children: none;
flow-children: right-wrap;
flow-children: down-wrap;
ignore-parent-flow
忽略父级组件的flow-children属性

overflow
指定如何处理溢出面板可用空间的内容。

属性值
值   描述
squish  默认。如果需要，会挤压子面板以适应面板的边界
clip    子元素保持他们想要的大小，但他们的内容被剪裁了
scroll  子元素保持其所需大小，并向此面板添加滚动条
noclip  子元素保持他们想要的大小，并且允许内容溢出此面板
范例
overflow: squish squish; // 在水平和垂直方向上挤压内容
overflow: squish scroll; // 沿Y方向滚动内容
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;


font
font 简写属性在一个声明中设置所有字体属性。

font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
font-stretch
使文字变宽或变窄。测试condensed会使英文字母与数字变窄。

属性值
值   描述
normal  默认。正常。
condensed   压缩
expanded    扩展
范例
font-stretch: normal;
font-stretch: condensed;
font-stretch: expanded;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
font-weight
设置文本的粗细。实际上测试发现中文字体没有适配该属性，英文字母与数字只有bold与black有效且都为粗体。

属性值
值   描述
light   更细的字体
thin    细的字体
normal  默认。正常
medium  中型字体
bold    粗体
black   更粗的字体
范例
font-weight: normal;
font-weight: bold;
font-weight: thin;

font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font
font 简写属性在一个声明中设置所有字体属性。

font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
font-stretch
使文字变宽或变窄。测试condensed会使英文字母与数字变窄。

属性值
值   描述
normal  默认。正常。
condensed   压缩
expanded    扩展
范例
font-stretch: normal;
font-stretch: condensed;
font-stretch: expanded;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
font-weight
设置文本的粗细。实际上测试发现中文字体没有适配该属性，英文字母与数字只有bold与black有效且都为粗体。

属性值
值   描述
light   更细的字体
thin    细的字体
normal  默认。正常
medium  中型字体
bold    粗体
black   更粗的字体
范例
font-weight: normal;
font-weight: bold;
font-weight: thin;

font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
font
font 简写属性在一个声明中设置所有字体属性。

font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-stretch
使文字变宽或变窄。测试condensed会使英文字母与数字变窄。

属性值
值   描述
normal  默认。正常。
condensed   压缩
expanded    扩展
范例
font-stretch: normal;
font-stretch: condensed;
font-stretch: expanded;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
font-weight
设置文本的粗细。实际上测试发现中文字体没有适配该属性，英文字母与数字只有bold与black有效且都为粗体。

属性值
值   描述
light   更细的字体
thin    细的字体
normal  默认。正常
medium  中型字体
bold    粗体
black   更粗的字体
范例
font-weight: normal;
font-weight: bold;
font-weight: thin;
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px

font-stretch
使文字变宽或变窄。测试condensed会使英文字母与数字变窄。

属性值
值   描述
normal  默认。正常。
condensed   压缩
expanded    扩展
范例
font-stretch: normal;
font-stretch: condensed;
font-stretch: expanded;
font
font 简写属性在一个声明中设置所有字体属性。

font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
font-weight
设置文本的粗细。实际上测试发现中文字体没有适配该属性，英文字母与数字只有bold与black有效且都为粗体。

属性值
值   描述
light   更细的字体
thin    细的字体
normal  默认。正常
medium  中型字体
bold    粗体
black   更粗的字体
范例
font-weight: normal;
font-weight: bold;
font-weight: thin;

font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
font
font 简写属性在一个声明中设置所有字体属性。

font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
font-stretch
使文字变宽或变窄。测试condensed会使英文字母与数字变窄。

属性值
值   描述
normal  默认。正常。
condensed   压缩
expanded    扩展
范例
font-stretch: normal;
font-stretch: condensed;
font-stretch: expanded;
font-weight
设置文本的粗细。实际上测试发现中文字体没有适配该属性，英文字母与数字只有bold与black有效且都为粗体。

属性值
值   描述
light   更细的字体
thin    细的字体
normal  默认。正常
medium  中型字体
bold    粗体
black   更粗的字体
范例
font-weight: normal;
font-weight: bold;
font-weight: thin;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;

font-weight
设置文本的粗细。实际上测试发现中文字体没有适配该属性，英文字母与数字只有bold与black有效且都为粗体。

属性值
值   描述
light   更细的字体
thin    细的字体
normal  默认。正常
medium  中型字体
bold    粗体
black   更粗的字体
范例
font-weight: normal;
font-weight: bold;
font-weight: thin;
font
font 简写属性在一个声明中设置所有字体属性。

font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
font-stretch
使文字变宽或变窄。测试condensed会使英文字母与数字变窄。

属性值
值   描述
normal  默认。正常。
condensed   压缩
expanded    扩展
范例
font-stretch: normal;
font-stretch: condensed;
font-stretch: expanded;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;

height
设置此面板的高度。可能的值： "fit-children" - 面板大小设置为所有子元素所需的大小(默认) - 任何固定像素值(例如：“100px”) - 父级高度百分比(例如：“100%”) "fill-parent-flow( )" - 填充到剩余的父级高度。如果将多个子项设置为此值，则使用权重来确定最终高度。例如，如果将三个子项设置为fill-parent-flow为1.0，并且父项的高度为300px，则每个子项的高度将为100px。(例如：“fill-parent-flow(1.0)”) "width-percentage( )" - 面板宽度的百分比，它允许您强制实施特定的纵横比。宽度不能也是高度的百分比。

属性值
值   描述
fit-children    默认。面板大小设置为所有子元素所需的大小
<pixels>    任何固定像素值
<percentage>    父级高度百分比
fill-parent-flow( <weight> )    填充到剩余的父级高度
width-percentage( <percentage> )    面板宽度的百分比
范例
height: fit-children;
height: 100px;
height: 100%;
height: width-percentage( 100% );
height: fill-parent-flow( 1.0 );
line-height
Specifies the line height (distance between top edge of line above and line below) to use for text. By default this is 'normal' and a value that matches the font-size reasonably will be used automatically. Unlike the web, we don't have a weird CSS inheritence problem with the 120% vs 1.2 styles.

范例
line-height: normal;
line-height: 20px;
line-height: 1.2;
line-height: 120%;
max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;
min-height
设置元素的最小高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
min-height: 100px;
min-height: 100%;

horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
align
面板对齐方式，第一个参数填水平对齐方式，第二个参数填垂直对齐方式

属性值
值   描述
top 上对齐
bottom  下对齐
left    左对齐
right   右对齐
center  居中对齐
范例
align: right center;    // 水平居右，垂直居中
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
vertical-align
垂直方向上的定位

属性值
值   描述
top 默认，顶部
bottom  底部
center  垂直居中
middle  等同于center
范例
vertical-align: bottom;

hue-rotation
Sets the hue rotation to apply to the panel and all it's children during composition. Default of 0.0 means no adjustment, domain is in degrees.

范例
hue-rotation: 180deg;

ignore-parent-flow
忽略父级组件的flow-children属性

flow-children
子元素的排版方式

属性值
值   描述
none    默认，无
right   水平排版
down    垂直排版
right-wrap  水平排版，超过父类边界的子元素自动换行
down-wrap   垂直排版，超过父类边界的子元素自动换行
范例
flow-children: down;
flow-children: right;
flow-children: none;
flow-children: right-wrap;
flow-children: down-wrap;
overflow
指定如何处理溢出面板可用空间的内容。

属性值
值   描述
squish  默认。如果需要，会挤压子面板以适应面板的边界
clip    子元素保持他们想要的大小，但他们的内容被剪裁了
scroll  子元素保持其所需大小，并向此面板添加滚动条
noclip  子元素保持他们想要的大小，并且允许内容溢出此面板
范例
overflow: squish squish; // 在水平和垂直方向上挤压内容
overflow: squish scroll; // 沿Y方向滚动内容
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;

img-shadow
Specifies image shadows. The shadow shape will match the image the panel can generate,and this is only meaningful for images. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
img-shadow: 2px 2px 8px 3.0 #333333b0;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
box-shadow
Specifies shadows for boxes, or inset shadows/glows. The shadow shape will match the border box for the panel,so use border-radius to affect rounding. Syntax takes optional shape 'inset', 'fill', or 'hollow', then color, horizontal offset pixels, vertical offset pixels, blur radius pixels, and spread distance in pixels. Inset means the shadow is an inner shadow/glow, fill is a a shadow behind the entire box, hollow means clipping it to outside the border area only (before offset). A negative blur radius will give a hard-edged look to the shadow, effectively a rounded outline of the same size as the blur.

范例
box-shadow: #ffffff80 4px 4px 8px 0px; // outer, filled
box-shadow: hollow #ffffff80 4px 4px 8px 0px; // outer, hollow
box-shadow: inset #333333b0 0px 0px 8px 12px; // inner
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;

layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;

letter-spacing
Sets letter-spacing for text in a string. Possible values: normal - no manual spacing <pixels> - Any fixed pixel value (ex: "1px")

line-height
Specifies the line height (distance between top edge of line above and line below) to use for text. By default this is 'normal' and a value that matches the font-size reasonably will be used automatically. Unlike the web, we don't have a weird CSS inheritence problem with the 120% vs 1.2 styles.

范例
line-height: normal;
line-height: 20px;
line-height: 1.2;
line-height: 120%;
height
设置此面板的高度。可能的值： "fit-children" - 面板大小设置为所有子元素所需的大小(默认) - 任何固定像素值(例如：“100px”) - 父级高度百分比(例如：“100%”) "fill-parent-flow( )" - 填充到剩余的父级高度。如果将多个子项设置为此值，则使用权重来确定最终高度。例如，如果将三个子项设置为fill-parent-flow为1.0，并且父项的高度为300px，则每个子项的高度将为100px。(例如：“fill-parent-flow(1.0)”) "width-percentage( )" - 面板宽度的百分比，它允许您强制实施特定的纵横比。宽度不能也是高度的百分比。

属性值
值   描述
fit-children    默认。面板大小设置为所有子元素所需的大小
<pixels>    任何固定像素值
<percentage>    父级高度百分比
fill-parent-flow( <weight> )    填充到剩余的父级高度
width-percentage( <percentage> )    面板宽度的百分比
范例
height: fit-children;
height: 100px;
height: 100%;
height: width-percentage( 100% );
height: fill-parent-flow( 1.0 );
max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;
min-height
设置元素的最小高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
min-height: 100px;
min-height: 100%;

margin
margin 简写属性在一个声明中设置所有外边距属性。该属性可以有 1 到 4 个值。

属性值
值   描述
<pixel> 任意固定值的像素
<percentage>    任意百分比
范例
margin: 10px 5px 15px 20px;    // 上边距10px，右边距5px，下边距15px，左边距20px
margin: 10px 5px 15px;    // 上边距10px，右边距和左边距是5px，下边距15px
margin: 10px 5px;    // 上边距和下边距10px，右边距和左边距是5px
margin: 10px;    // 所有边距都是10px
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;

margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
margin
margin 简写属性在一个声明中设置所有外边距属性。该属性可以有 1 到 4 个值。

属性值
值   描述
<pixel> 任意固定值的像素
<percentage>    任意百分比
范例
margin: 10px 5px 15px 20px;    // 上边距10px，右边距5px，下边距15px，左边距20px
margin: 10px 5px 15px;    // 上边距10px，右边距和左边距是5px，下边距15px
margin: 10px 5px;    // 上边距和下边距10px，右边距和左边距是5px
margin: 10px;    // 所有边距都是10px
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
padding-bottom
<Needs a description>

margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
margin
margin 简写属性在一个声明中设置所有外边距属性。该属性可以有 1 到 4 个值。

属性值
值   描述
<pixel> 任意固定值的像素
<percentage>    任意百分比
范例
margin: 10px 5px 15px 20px;    // 上边距10px，右边距5px，下边距15px，左边距20px
margin: 10px 5px 15px;    // 上边距10px，右边距和左边距是5px，下边距15px
margin: 10px 5px;    // 上边距和下边距10px，右边距和左边距是5px
margin: 10px;    // 所有边距都是10px
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
padding-left
<Needs a description>

margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
margin
margin 简写属性在一个声明中设置所有外边距属性。该属性可以有 1 到 4 个值。

属性值
值   描述
<pixel> 任意固定值的像素
<percentage>    任意百分比
范例
margin: 10px 5px 15px 20px;    // 上边距10px，右边距5px，下边距15px，左边距20px
margin: 10px 5px 15px;    // 上边距10px，右边距和左边距是5px，下边距15px
margin: 10px 5px;    // 上边距和下边距10px，右边距和左边距是5px
margin: 10px;    // 所有边距都是10px
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
padding-right
<Needs a description>

margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;
margin
margin 简写属性在一个声明中设置所有外边距属性。该属性可以有 1 到 4 个值。

属性值
值   描述
<pixel> 任意固定值的像素
<percentage>    任意百分比
范例
margin: 10px 5px 15px 20px;    // 上边距10px，右边距5px，下边距15px，左边距20px
margin: 10px 5px 15px;    // 上边距10px，右边距和左边距是5px，下边距15px
margin: 10px 5px;    // 上边距和下边距10px，右边距和左边距是5px
margin: 10px;    // 所有边距都是10px
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;
border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
padding-top
<Needs a description>

max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;
max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
height
设置此面板的高度。可能的值： "fit-children" - 面板大小设置为所有子元素所需的大小(默认) - 任何固定像素值(例如：“100px”) - 父级高度百分比(例如：“100%”) "fill-parent-flow( )" - 填充到剩余的父级高度。如果将多个子项设置为此值，则使用权重来确定最终高度。例如，如果将三个子项设置为fill-parent-flow为1.0，并且父项的高度为300px，则每个子项的高度将为100px。(例如：“fill-parent-flow(1.0)”) "width-percentage( )" - 面板宽度的百分比，它允许您强制实施特定的纵横比。宽度不能也是高度的百分比。

属性值
值   描述
fit-children    默认。面板大小设置为所有子元素所需的大小
<pixels>    任何固定像素值
<percentage>    父级高度百分比
fill-parent-flow( <weight> )    填充到剩余的父级高度
width-percentage( <percentage> )    面板宽度的百分比
范例
height: fit-children;
height: 100px;
height: 100%;
height: width-percentage( 100% );
height: fill-parent-flow( 1.0 );
line-height
Specifies the line height (distance between top edge of line above and line below) to use for text. By default this is 'normal' and a value that matches the font-size reasonably will be used automatically. Unlike the web, we don't have a weird CSS inheritence problem with the 120% vs 1.2 styles.

范例
line-height: normal;
line-height: 20px;
line-height: 1.2;
line-height: 120%;
min-height
设置元素的最小高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
min-height: 100px;
min-height: 100%;

max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

min-height
设置元素的最小高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
min-height: 100px;
min-height: 100%;
animation-timing-function
<Needs a description>

min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );
height
设置此面板的高度。可能的值： "fit-children" - 面板大小设置为所有子元素所需的大小(默认) - 任何固定像素值(例如：“100px”) - 父级高度百分比(例如：“100%”) "fill-parent-flow( )" - 填充到剩余的父级高度。如果将多个子项设置为此值，则使用权重来确定最终高度。例如，如果将三个子项设置为fill-parent-flow为1.0，并且父项的高度为300px，则每个子项的高度将为100px。(例如：“fill-parent-flow(1.0)”) "width-percentage( )" - 面板宽度的百分比，它允许您强制实施特定的纵横比。宽度不能也是高度的百分比。

属性值
值   描述
fit-children    默认。面板大小设置为所有子元素所需的大小
<pixels>    任何固定像素值
<percentage>    父级高度百分比
fill-parent-flow( <weight> )    填充到剩余的父级高度
width-percentage( <percentage> )    面板宽度的百分比
范例
height: fit-children;
height: 100px;
height: 100%;
height: width-percentage( 100% );
height: fill-parent-flow( 1.0 );
line-height
Specifies the line height (distance between top edge of line above and line below) to use for text. By default this is 'normal' and a value that matches the font-size reasonably will be used automatically. Unlike the web, we don't have a weird CSS inheritence problem with the 120% vs 1.2 styles.

范例
line-height: normal;
line-height: 20px;
line-height: 1.2;
line-height: 120%;
max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;

min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;
animation-timing-function
<Needs a description>

min-height
设置元素的最小高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
min-height: 100px;
min-height: 100%;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
opacity-mask-scale

opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
opacity-mask-scale
border-brush
EXPERIMENTAL: Sets a more-complex brush for the entire border paint area (ignores normal border color).

范例
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
border-brush: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
border-brush: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );

opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask-position
opacity-mask-scale

opacity-mask-position
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-scale
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;

opacity-mask-scale
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
ui-scale
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-x
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-y
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.

overflow
指定如何处理溢出面板可用空间的内容。

属性值
值   描述
squish  默认。如果需要，会挤压子面板以适应面板的边界
clip    子元素保持他们想要的大小，但他们的内容被剪裁了
scroll  子元素保持其所需大小，并向此面板添加滚动条
noclip  子元素保持他们想要的大小，并且允许内容溢出此面板
范例
overflow: squish squish; // 在水平和垂直方向上挤压内容
overflow: squish scroll; // 沿Y方向滚动内容
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;

padding
<Needs a description>

padding-bottom
<Needs a description>

padding-left
<Needs a description>

padding-right
<Needs a description>

padding-top
<Needs a description>

padding-bottom
<Needs a description>

padding
<Needs a description>

padding-left
<Needs a description>

padding-right
<Needs a description>

padding-top
<Needs a description>

border-bottom
Shorthand for setting the bottom panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-bottom: 2px solid #111111FF;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
margin-bottom
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-bottom: 100px;
margin-bottom: 10%;

padding-left
<Needs a description>

padding
<Needs a description>

padding-bottom
<Needs a description>

padding-right
<Needs a description>

padding-top
<Needs a description>

border-bottom-left-radius
Specifies border-radius for bottom-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-left-radius: 2px 2px;
border-bottom-left-radius: 5%;
border-left
Shorthand for setting the left panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-left: 2px solid #111111FF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
margin-left
左边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-left: 100px;
margin-left: 10%;

padding-right
<Needs a description>

padding
<Needs a description>

padding-bottom
<Needs a description>

padding-left
<Needs a description>

padding-top
<Needs a description>

border-bottom-right-radius
Specifies border-radius for bottom-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-bottom-right-radius: 2px 2px;
border-bottom-right-radius: 5%;
border-right
Shorthand for setting the right panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-right: 2px solid #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
brightness
Sets the brightness that applies to the panel and all it's children during composition. The value is a multiplier on the HSB brightness value.

范例
brightness: 1.5;
margin-right
右边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-right: 100px;
margin-right: 10%;

padding-top
<Needs a description>

padding
<Needs a description>

padding-bottom
<Needs a description>

padding-left
<Needs a description>

padding-right
<Needs a description>

border-top
Shorthand for setting the top panel border. Specify width, style, and color. Supported styles are: solid, dashed, none.

范例
border-top: 2px solid #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
border-top-left-radius
Specifies border-radius for top-left corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-left-radius: 2px 2px;
border-top-left-radius: 5%;
border-top-right-radius
Specifies border-radius for top-right corner which rounds off border and clips background/foreground content to rounded edge. Takes 1 or 2 values in px or %, first value is horizontal radii for elliptical corner, second is vertical radii, if only one is specified then horizontal/vertical will both be set and corner will be circular.

范例
border-top-right-radius: 2px 2px;
border-top-right-radius: 5%;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
margin-top
下边距。负值是允许的。

属性值
值   描述
<pixel> 任意固定像素值
<percentage>    任意百分比
范例
margin-top: 100px;
margin-top: 10%;

perspective
Sets the perspective depth space available for children of the panel. Default of 1000 would mean that children at 1000px zpos are right at the viewers eye, -1000px are just out of view distance faded to nothing.

范例
perspective: 1000;
perspective-origin
Sets the perspective origin which will be used when transforming children of this panel. This can be thought of as the camera x/y position relative to the panel.

范例
perspective-origin: 50% 50%;

perspective-origin
Sets the perspective origin which will be used when transforming children of this panel. This can be thought of as the camera x/y position relative to the panel.

范例
perspective-origin: 50% 50%;
perspective
Sets the perspective depth space available for children of the panel. Default of 1000 would mean that children at 1000px zpos are right at the viewers eye, -1000px are just out of view distance faded to nothing.

范例
perspective: 1000;
transform-origin
Sets the transform origin about which transforms will be applied. Default is 50% 50% on the panel so a rotation/scale is centered.

范例
transform-origin: 50% 50%

position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;

pre-transform-rotate2d
Sets 2 dimensional rotation degrees that apply to the quad for this panel prior to 3 dimensional transforms. This rotation applies without perspective and leaves the panel centered at the same spot as it started.

范例
pre-transform-rotate2d: 45deg;
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
transform
Sets the transforms to apply to the panel in 2d or 3d space. You can combine various transforms (comma separated) and they will be applied in order to create a 4x4 3d transform matrix. The possible operations are: translate3d( x, y, z ), translatex( x ), translatey( y ), translatez( z ), scale3d( x, y, z), rotate3d( x, y, z ), rotatex( x ), rotatey( y ), rotatez( z ).

范例
transform: translate3d( -100px, -100px, 0px );
transform: rotateZ( -32deg ) rotateX( 30deg ) translate3d( 125px, 520px, 230px );
transform-origin
Sets the transform origin about which transforms will be applied. Default is 50% 50% on the panel so a rotation/scale is centered.

范例
transform-origin: 50% 50%

pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
pre-transform-rotate2d
Sets 2 dimensional rotation degrees that apply to the quad for this panel prior to 3 dimensional transforms. This rotation applies without perspective and leaves the panel centered at the same spot as it started.

范例
pre-transform-rotate2d: 45deg;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
transform
Sets the transforms to apply to the panel in 2d or 3d space. You can combine various transforms (comma separated) and they will be applied in order to create a 4x4 3d transform matrix. The possible operations are: translate3d( x, y, z ), translatex( x ), translatey( y ), translatez( z ), scale3d( x, y, z), rotate3d( x, y, z ), rotatex( x ), rotatey( y ), rotatez( z ).

范例
transform: translate3d( -100px, -100px, 0px );
transform: rotateZ( -32deg ) rotateX( 30deg ) translate3d( 125px, 520px, 230px );
transform-origin
Sets the transform origin about which transforms will be applied. Default is 50% 50% on the panel so a rotation/scale is centered.

范例
transform-origin: 50% 50%

saturation
设置饱和度的值，以适用于面板和所有它的子元素在合成。默认值1.0表示没有调整，0.0表示完全不饱和到灰度，大于1.0表示过饱和。

范例
saturation: 0.4;

sound
Specifies a sound name to play when this selector is applied.

范例
sound: "whoosh_in";
sound-out
Specifies a sound name to play when this selector is removed.

范例
sound-out: "whoosh_out";

sound-out
Specifies a sound name to play when this selector is removed.

范例
sound-out: "whoosh_out";
sound
Specifies a sound name to play when this selector is applied.

范例
sound: "whoosh_in";
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;

text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
align
面板对齐方式，第一个参数填水平对齐方式，第二个参数填垂直对齐方式

属性值
值   描述
top 上对齐
bottom  下对齐
left    左对齐
right   右对齐
center  居中对齐
范例
align: right center;    // 水平居右，垂直居中
horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
vertical-align
垂直方向上的定位

属性值
值   描述
top 默认，顶部
bottom  底部
center  垂直居中
middle  等同于center
范例
vertical-align: bottom;

text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;

text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;

text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
overflow
指定如何处理溢出面板可用空间的内容。

属性值
值   描述
squish  默认。如果需要，会挤压子面板以适应面板的边界
clip    子元素保持他们想要的大小，但他们的内容被剪裁了
scroll  子元素保持其所需大小，并向此面板添加滚动条
noclip  子元素保持他们想要的大小，并且允许内容溢出此面板
范例
overflow: squish squish; // 在水平和垂直方向上挤压内容
overflow: squish scroll; // 沿Y方向滚动内容

text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
box-shadow
Specifies shadows for boxes, or inset shadows/glows. The shadow shape will match the border box for the panel,so use border-radius to affect rounding. Syntax takes optional shape 'inset', 'fill', or 'hollow', then color, horizontal offset pixels, vertical offset pixels, blur radius pixels, and spread distance in pixels. Inset means the shadow is an inner shadow/glow, fill is a a shadow behind the entire box, hollow means clipping it to outside the border area only (before offset). A negative blur radius will give a hard-edged look to the shadow, effectively a rounded outline of the same size as the blur.

范例
box-shadow: #ffffff80 4px 4px 8px 0px; // outer, filled
box-shadow: hollow #ffffff80 4px 4px 8px 0px; // outer, hollow
box-shadow: inset #333333b0 0px 0px 8px 12px; // inner
img-shadow
Specifies image shadows. The shadow shape will match the image the panel can generate,and this is only meaningful for images. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
img-shadow: 2px 2px 8px 3.0 #333333b0;

text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
pre-transform-rotate2d
Sets 2 dimensional rotation degrees that apply to the quad for this panel prior to 3 dimensional transforms. This rotation applies without perspective and leaves the panel centered at the same spot as it started.

范例
pre-transform-rotate2d: 45deg;
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
transform
Sets the transforms to apply to the panel in 2d or 3d space. You can combine various transforms (comma separated) and they will be applied in order to create a 4x4 3d transform matrix. The possible operations are: translate3d( x, y, z ), translatex( x ), translatey( y ), translatez( z ), scale3d( x, y, z), rotate3d( x, y, z ), rotatex( x ), rotatey( y ), rotatez( z ).

范例
transform: translate3d( -100px, -100px, 0px );
transform: rotateZ( -32deg ) rotateX( 30deg ) translate3d( 125px, 520px, 230px );
transform-origin
Sets the transform origin about which transforms will be applied. Default is 50% 50% on the panel so a rotation/scale is centered.

范例
transform-origin: 50% 50%

texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px


tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;

tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;

tooltip-position
Specifies where to position a tooltip relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the tooltip doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the tooltip first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
tooltip-position: bottom;
tooltip-position: left bottom;
tooltip-arrow-position
Specifies where to point the arrow of a tooltip at on this panel. The first value controls how the arrow is positioned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the tooltip is to the left or right of the panel. Default is '50% 50%'.

范例
tooltip-arrow-position: 25% 50%;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
background-position
Controls the horizontal and vertical placement of the background image, with the format: <left|center|right> <horizontal length> <top|center|bottom> <vertical length> If length is a percent, the specified location within the image is positioned over that same specified position in the background. If the length is pixels, the top left corner is placed relative to the provided alignment keywords (left, bottom, etc.). See examples for more details.

If 1 value is specified, the other value is assumed to be center. If 2 values are specified, the first value must be for horizontal placement and the second for vertical.

范例
// aligns the top left corner of the image with the top left corner of the panel (default)
background-position: 0% 0%;
// centers the image within the background (same as "center center")
background-position: center;

// aligns the bottom right corner of the image with the bottom right corner of the panel (same as "100% 100%")
background-position: right bottom;

// the top left corner of the image is placed 10px to the right of, 40px below the top left corner of the panel
background-position: left 10px top 40px;
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity-mask-position
position
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;

transform
Sets the transforms to apply to the panel in 2d or 3d space. You can combine various transforms (comma separated) and they will be applied in order to create a 4x4 3d transform matrix. The possible operations are: translate3d( x, y, z ), translatex( x ), translatey( y ), translatez( z ), scale3d( x, y, z), rotate3d( x, y, z ), rotatex( x ), rotatey( y ), rotatez( z ).

范例
transform: translate3d( -100px, -100px, 0px );
transform: rotateZ( -32deg ) rotateX( 30deg ) translate3d( 125px, 520px, 230px );
pre-transform-rotate2d
Sets 2 dimensional rotation degrees that apply to the quad for this panel prior to 3 dimensional transforms. This rotation applies without perspective and leaves the panel centered at the same spot as it started.

范例
pre-transform-rotate2d: 45deg;
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
transform-origin
Sets the transform origin about which transforms will be applied. Default is 50% 50% on the panel so a rotation/scale is centered.

范例
transform-origin: 50% 50%


transform-origin
Sets the transform origin about which transforms will be applied. Default is 50% 50% on the panel so a rotation/scale is centered.

范例
transform-origin: 50% 50%
pre-transform-rotate2d
Sets 2 dimensional rotation degrees that apply to the quad for this panel prior to 3 dimensional transforms. This rotation applies without perspective and leaves the panel centered at the same spot as it started.

范例
pre-transform-rotate2d: 45deg;
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
transform
Sets the transforms to apply to the panel in 2d or 3d space. You can combine various transforms (comma separated) and they will be applied in order to create a 4x4 3d transform matrix. The possible operations are: translate3d( x, y, z ), translatex( x ), translatey( y ), translatez( z ), scale3d( x, y, z), rotate3d( x, y, z ), rotatex( x ), rotatey( y ), rotatez( z ).

范例
transform: translate3d( -100px, -100px, 0px );
transform: rotateZ( -32deg ) rotateX( 30deg ) translate3d( 125px, 520px, 230px );
perspective-origin
Sets the perspective origin which will be used when transforming children of this panel. This can be thought of as the camera x/y position relative to the panel.

范例
perspective-origin: 50% 50%;

transition
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles. Also specifies duration, timing function, and delay. Valid timing functions are: ease, ease-in, ease-out, ease-in-out, linear.

范例
transition: position 2.0s ease-in-out 0.0s, perspective-origin 1.2s ease-in-out 0.8s;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;
transition-high-framerate
Specifies the desire for higher framerate during this transition, if we have control.

范例
transition-high-framerate: true;
transition-high-framerate: false, true, false;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );

transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles. Also specifies duration, timing function, and delay. Valid timing functions are: ease, ease-in, ease-out, ease-in-out, linear.

范例
transition: position 2.0s ease-in-out 0.0s, perspective-origin 1.2s ease-in-out 0.8s;
transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;
transition-high-framerate
Specifies the desire for higher framerate during this transition, if we have control.

范例
transition-high-framerate: true;
transition-high-framerate: false, true, false;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );
animation-delay
<Needs a description>

transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;
transition
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles. Also specifies duration, timing function, and delay. Valid timing functions are: ease, ease-in, ease-out, ease-in-out, linear.

范例
transition: position 2.0s ease-in-out 0.0s, perspective-origin 1.2s ease-in-out 0.8s;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-high-framerate
Specifies the desire for higher framerate during this transition, if we have control.

范例
transition-high-framerate: true;
transition-high-framerate: false, true, false;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );
animation-duration
定义一个动画播放的时间

transition-high-framerate
Specifies the desire for higher framerate during this transition, if we have control.

范例
transition-high-framerate: true;
transition-high-framerate: false, true, false;
transition
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles. Also specifies duration, timing function, and delay. Valid timing functions are: ease, ease-in, ease-out, ease-in-out, linear.

范例
transition: position 2.0s ease-in-out 0.0s, perspective-origin 1.2s ease-in-out 0.8s;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );

transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
transition
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles. Also specifies duration, timing function, and delay. Valid timing functions are: ease, ease-in, ease-out, ease-in-out, linear.

范例
transition: position 2.0s ease-in-out 0.0s, perspective-origin 1.2s ease-in-out 0.8s;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;
transition-high-framerate
Specifies the desire for higher framerate during this transition, if we have control.

范例
transition-high-framerate: true;
transition-high-framerate: false, true, false;
transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );

transition-timing-function
指定用于此面板上的转换属性的计时函数，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。有效的定时函数有:ease, ease-in, ease-out, ease-in-out，linear。

属性值
值   描述
ease    慢速开始，然后变快，然后慢速结束的过渡效果
ease-in 以慢速开始的过渡效果
ease-out    以慢速结束的过渡效果
ease-in-out 以慢速开始和结束的过渡效果
linear  以相同速度开始至结束的过渡效果
cubic-bezier(n,n,n,n)   在 cubic-bezier 函数中定义自己的值。可能的值是 0 至 1 之间的数值。
范例
transition-timing-function: ease-in-out;
transition-timing-function: ease-in-out, linear;
transition-timing-function: cubic-bezier( 0.785, 0.385, 0.555, 1.505 );
transition
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles. Also specifies duration, timing function, and delay. Valid timing functions are: ease, ease-in, ease-out, ease-in-out, linear.

范例
transition: position 2.0s ease-in-out 0.0s, perspective-origin 1.2s ease-in-out 0.8s;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-duration
指定此面板上转换属性使用的持续时间(以秒为单位)，如果指定了多个以逗号分隔的值，则按顺序将值应用于“transition-property”中指定的每个属性。如果只指定了一个值，那么它将应用于在transition-property中指定的所有属性。

范例
transition-duration: 2.0s;
transition-duration: 2.0s, 1.2s, 1.2s, 4.0s, 2.0s;
transition-high-framerate
Specifies the desire for higher framerate during this transition, if we have control.

范例
transition-high-framerate: true;
transition-high-framerate: false, true, false;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
animation-timing-function
<Needs a description>

ui-scale
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-x
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-y
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
opacity-mask-scale
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6

ui-scale-x
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-y
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
opacity-mask-scale
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
box-shadow
Specifies shadows for boxes, or inset shadows/glows. The shadow shape will match the border box for the panel,so use border-radius to affect rounding. Syntax takes optional shape 'inset', 'fill', or 'hollow', then color, horizontal offset pixels, vertical offset pixels, blur radius pixels, and spread distance in pixels. Inset means the shadow is an inner shadow/glow, fill is a a shadow behind the entire box, hollow means clipping it to outside the border area only (before offset). A negative blur radius will give a hard-edged look to the shadow, effectively a rounded outline of the same size as the blur.

范例
box-shadow: #ffffff80 4px 4px 8px 0px; // outer, filled
box-shadow: hollow #ffffff80 4px 4px 8px 0px; // outer, hollow
box-shadow: inset #333333b0 0px 0px 8px 12px; // inner
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;
max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
x
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
z-index
设置面板的z-index，面板将在父级面板中按顺序排序和绘制。排序首先根据位置计算的z-pos进行排序，然后转换，如果面板有匹配的zpos，则使用zindex。z-index与z-pos的不同之处在于，它不影响渲染透视图，只影响绘制/命中测试顺序。默认的z-index值是0，并且可以接受任何浮点值。

范例
z-index: 1;

ui-scale-y
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-x
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
opacity-mask-scale
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
animation-delay
<Needs a description>

background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
visibility
控制面板是否可见并包含在面板布局中。

属性值
值   描述
visible 默认，可见
collapse    不可见并且不包含在面板布局中
范例
visibility: collapse;
y
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;

ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-x
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
ui-scale-y
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
opacity-mask-scale
pre-transform-scale2d
Sets 2 dimensional X/Y scale factors that apply to the quad for this panel prior to 3 dimensional transforms. This scaling applies without perspective and leaves the panel centered at the same spot as it started. Default of 1.0 means no scaling, 0.5 would be half size.

范例
pre-transform-scale2d: 0.8
pre-transform-scale2d: 0.4, 0.6
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
z
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
z-index
设置面板的z-index，面板将在父级面板中按顺序排序和绘制。排序首先根据位置计算的z-pos进行排序，然后转换，如果面板有匹配的zpos，则使用zindex。z-index与z-pos的不同之处在于，它不影响渲染透视图，只影响绘制/命中测试顺序。默认的z-index值是0，并且可以接受任何浮点值。

范例
z-index: 1;

vertical-align
垂直方向上的定位

属性值
值   描述
top 默认，顶部
bottom  底部
center  垂直居中
middle  等同于center
范例
vertical-align: bottom;
align
面板对齐方式，第一个参数填水平对齐方式，第二个参数填垂直对齐方式

属性值
值   描述
top 上对齐
bottom  下对齐
left    左对齐
right   右对齐
center  居中对齐
范例
align: right center;    // 水平居右，垂直居中
horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;

visibility
控制面板是否可见并包含在面板布局中。

属性值
值   描述
visible 默认，可见
collapse    不可见并且不包含在面板布局中
范例
visibility: collapse;

wash-color
Specifies a 'wash' color, which means a color that will be blended over the panel and all it's children at composition time, tinting them. The alpha value of the color determines the intensity of the tinting.

范例
wash-color: #39b0d325;
background-color
设置面板的背景填充颜色/渐变/组合。

范例
background-color: #FFFFFFFF;
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), to( #c0c0c0c0 ) );
background-color: gradient( linear, 0% 0%, 0% 100%, from( #fbfbfbff ), color-stop( 0.3, #ebebebff ), to( #c0c0c0c0 ) );
background-color: gradient( radial, 50% 50%, 0% 0%, 80% 80%, from( #00ff00ff ), to( #0000ffff ) );
background-color: #0d1c22ff, gradient( radial, 100% -0%, 100px -40px, 320% 270%, from( #3a464bff ), color-stop( 0.23, #0d1c22ff ), to( #0d1c22ff ) );
background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
border-bottom-color
Specifies border color for the bottom edge of the panel.

范例
border-bottom-color: #111111FF;
border-color
Specifies border color for panel. If a single color value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-color: #111111FF;
border-color: #FF0000FF #00FF00FF #0000FFFF #00FFFFFF;
border-left-color
Specifies border color for the left edge of the panel.

范例
border-left-color: #111111FF;
border-right-color
Specifies border color for the right edge of the panel.

范例
border-right-color: #111111FF;
border-top-color
Specifies border color for the top edge of the panel.

范例
border-top-color: #111111FF;
color
设置面板的前景色填充颜色/渐变/组合。此颜色是用于在面板中渲染文本的颜色。

范例
color: #FFFFFFFF;
color: gradient( linear, 0% 0%, 0% 100%, from( #cbcbcbff ), to( #a0a0a0a0 ) );

white-space
Controls white-space wrapping on rendered text. "normal" means wrap on whitespace, "nowrap" means do no wrapping at all.

范例
white-space: normal;
white-space: nowrap;

width
Sets the width for this panel. Possible values: "fit-children" - Panel size is set to the required size of all children (default) <pixels> - Any fixed pixel value (ex: "100px") <percentage> - Percentage of parent width (ex: "100%") "fill-parent-flow( <weight> )" - Fills to remaining parent width. If multiple children are set to this value, weight is used to determine final width. For example, if three children are set to fill-parent-flow of 1.0 and the parent is 300px wide, each child will be 100px wide. (ex: "fill-parent-flow( 1.0 )" ) "height-percentage( <percentage> )" - Percentage of the panel's height, which allows you to enforce a particular aspect ratio. The height cannot also be width-percentage.

border-bottom-width
Specifies border width for the bottom edge of the panel.

范例
border-bottom-width: 2px;
border-left-width
Specifies border width for the left edge of the panel.

范例
border-left-width: 2px;
border-right-width
Specifies border width for the right edge of the panel.

范例
border-right-width: 2px;
border-top-width
Specifies border width for the top edge of the panel.

范例
border-top-width: 2px;
border-width
Specifies border width for panel. If a single width value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-width: 1px;'
border-width: 20px 1px 20px 1px;
max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
min-width
设置元素的最小宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
min-width: 100px;
min-width: 100%;

world-blur
Sets the amount of blur to apply to the world / backbuffer before drawing.

范例
world-blur: gaussian( 2.5 );
world-blur: gaussian( 6, 6, 1 );
world-blur: mipmapgaussian( 6, 6, 4 );  In this version each gaussian pass is preceded by a quarter area downsample.
background-blur
Sets the amount of blur to apply to the contents behind this panel during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
background-blur: gaussian( 2.5 );
background-blur: gaussian( 6, 6, 1 );
blur
Sets the amount of blur to apply to the panel and all it's children during composition. Default is no blur, for now Gaussian is the only blur type and takes a horizontal standard deviation, vertical standard deviation, and number of passes. Good std deviation values are around 0-10, if 10 is still not intense enough consider more passes, but more than one pass is bad for perf. As shorthand you can specify with just one value, which will be used for the standard deviation in both directions and 1 pass will be set.

范例
blur: gaussian( 2.5 );
blur: gaussian( 6, 6, 1 );

x
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
box-shadow
Specifies shadows for boxes, or inset shadows/glows. The shadow shape will match the border box for the panel,so use border-radius to affect rounding. Syntax takes optional shape 'inset', 'fill', or 'hollow', then color, horizontal offset pixels, vertical offset pixels, blur radius pixels, and spread distance in pixels. Inset means the shadow is an inner shadow/glow, fill is a a shadow behind the entire box, hollow means clipping it to outside the border area only (before offset). A negative blur radius will give a hard-edged look to the shadow, effectively a rounded outline of the same size as the blur.

范例
box-shadow: #ffffff80 4px 4px 8px 0px; // outer, filled
box-shadow: hollow #ffffff80 4px 4px 8px 0px; // outer, hollow
box-shadow: inset #333333b0 0px 0px 8px 12px; // inner
context-menu-arrow-position
Specifies where to point the arrow of a context menu at on this panel. The first value controls how the arrow is positioned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the arrow is positioned vertically when the context menu is to the left or right of the panel. Default is '50% 50%'.

范例
context-menu-arrow-position: 25% 50%;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
context-menu-position
Specifies where to position a context menu relative to this panel. Valid options include 'left', 'top', 'right', and 'bottom'. List up to 4 positions to determine the order that positions are tried if the context menu doesn't fully fit on screen. Default is 'right left bottom top'. If less than 4 positions are specified, the context menu first tries the opposite of the specified position along the same axis before switching to the other axis.

范例
context-menu-position: bottom;
context-menu-position: left bottom;
max-height
设置元素的最大高度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级高度百分比
范例
max-height: 100px;
max-height: 100%;
max-width
设置元素的最大宽度。

属性值
值   描述
<pixels>    任何固定像素值
<percentage>    父级宽度百分比
范例
max-width: 100px;
max-width: 100%;
text-align
指定此面板内文本的对齐方式，默认为左对齐。

属性值
值   描述
left    默认。左对齐
right   右对齐
center  居中对齐
justify 两端对齐
justify-letter-spacing  两端对齐并且自动调整字母间距
范例
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
text-align: justify-letter-spacing;
text-decoration
指定面板内文本的装饰，默认为无。可能的值:none, underline, line-through。

属性值
值   描述
none    默认。
underline   下划线
line-through    划掉
范例
text-decoration: underline;
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
text-overflow
当文本溢出包含元素时发生的事情。"clip"意味着简单地截断(在字符边界上)，"ellipsis"意味着以"…"结尾，而"shrink"的意思是缩小字体大小以适应。"noclip"允许基于"overflow"样式的文本溢出。我们默认使用"ellipsis"，这与正常的CSS规范相反。

属性值
值   描述
ellipsis    默认。以省略号结尾
clip    截断
shrink  自适应
noclip  溢出
范例
text-overflow: ellipsis;
text-overflow: clip;
text-overflow: shrink;
text-overflow: noclip;
text-shadow
Specifies text shadows. The shadow shape will match the text the panel can generate,and this is only meaningful for labels. Syntax takes horizontal offset pixels, vertical offset pixels, blur radius pixels, strength, and then shadow color.

范例
text-shadow: 2px 2px 8px 3.0 #333333b0;
text-transform
Specifies the transform for text within this panel, defaults to none. Possible values: none, uppercase, lowercase.

范例
text-transform: uppercase;
texture-sampling
Controls texture sampling mode for the panel. Set to alpha-only to use the textures alpha data across all 3 color channels, or point for point sampling.

范例
texture-sampling: normal;
texture-sampling: alpha-only;
texture-sampling: point;
ui-scale-x
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
z-index
设置面板的z-index，面板将在父级面板中按顺序排序和绘制。排序首先根据位置计算的z-pos进行排序，然后转换，如果面板有匹配的zpos，则使用zindex。z-index与z-pos的不同之处在于，它不影响渲染透视图，只影响绘制/命中测试顺序。默认的z-index值是0，并且可以接受任何浮点值。

范例
z-index: 1;

y
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
animation-delay
<Needs a description>

background-color-opacity
Sets the background color opacity for a panel (does nothing on its own, but when merged with a full background-color it overrides the opacity).

范例
background-color-opacity: 0.5;
background-img-opacity
Sets the Opacity of background-image

范例
background-img-opacity: 0.5;
border-bottom-style
Specifies border style for the bottom edge of the panel.

范例
border-bottom-style: solid;
border-left-style
Specifies border style for the left edge of the panel.

范例
border-left-style: solid;
border-right-style
Specifies border style for the right edge of the panel.

范例
border-right-style: solid;
border-style
Specifies border style for panel. If a single style value is set it applies to all sides, if 2 are set the first is top/bottom and the second is left/right, if all four are set then they are top, right, bottom, left in order.

范例
border-style: dashed;
border-style: solid none solid none;
border-top-style
Specifies border style for the top edge of the panel.

范例
border-top-style: solid;
context-menu-body-position
Specifies where to position the body of a context menu relative to this panel. The first value controls how the body is aligned horizontally when the context menu is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the context menu is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
context-menu-body-position: 50% 100%;
font-family
设置字体。可以把多个字体名称作为一个“回退”系统来保存。如果不支持第一个字体，则会尝试下一个。dota2中无法添加自己的字体，只能覆盖本地自嗨。如果引用了dotastyles.vcss则还有defaultFont、monospaceNumbersFont、titleFont、monospaceFont、diretideFont可以使用。

属性值
值   描述
Radiance    Radiance
FZLanTingHei-R-GBK  FZLanTingHei-R-GBK
TH Sarabun New  TH Sarabun New
YDYGO 540   YDYGO 540
Gulim   Gulim
MingLiU MingLiU
RadianceM   RadianceM
Reaver  Reaver
Goudy Trajan Medium Goudy Trajan Medium
FZKai-Z03   方正楷体
Courier New Courier New
Courier Courier
Creepster   Creepster
Valve Radus Valve Radus
范例
font-family: Courier New,Courier;
font-family: titleFont;    // 楷体
font-family: diretideFont;    // 夜魇暗潮样式
font-style
文本的字体样式。中文不支持斜体。

属性值
值   描述
normal  默认。正常
italic  斜体
范例
font-style: normal;
layout-position
Sets how the panel is positioned relative to its parent. "static" means position in the default way. "fixed" means position in the default way, but ignoring the parent's scroll offset.

范例
layout-position: fixed;
opacity
设置不透明度或透明度的值应用到面板和所有它的子元素。默认值1.0表示完全不透明，0.0表示完全透明。

范例
opacity: 0.8;
opacity-brush
Sets an opacity brush to apply to the panel and all it's children during composition.

范例
opacity-brush: gradient( linear, 0% 0%, 0% 100%, from( #ffffffff ), to( #ffffff00 ) );
opacity-mask
undefined

范例
opacity-mask: url( "file://{images}/upper_row_mask.tga" );
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) 0.5;
opacity-mask: url( "file://{images}/upper_row_mask.tga" ) -1.0;
opacity-mask-position: 5px 50%;
opacity-mask-scale: 200%;
opacity-mask-scale: 50% 100%;
opacity-mask-position
opacity-mask-scale
text-decoration-style
Specifies the decoration style for text within this panel, defaults to none. Possible values: none, dashed, dotted, wavy.

范例
text-decoration-style: dotted;
tooltip-body-position
Specifies where to position the body of a tooltip relative to this panel. The first value controls how the body is aligned horizontally when the tooltip is to the top or bottom of the panel, and the second value controls how the body is aligned vertically when the tooltip is to the left or right of the panel. 0% means left/top aligned, 50% means center/middle aligned, and 100% means right/bottom aligned. Default is '50% 50%'.

范例
tooltip-body-position: 50% 100%;
transition-delay
Specifies the delay in seconds to use for transition properties on this panel, if more than one comma delimited value is specified then the values are applied to each property specified in 'transition-property' in order. If only one value is specified then it applies to all the properties specified in transition-property.

范例
transition-delay: 0.0s;
transition-delay: 0.0s, 1.2s;
transition-property
Specifies which properties should transition smoothly to new values if a class/pseudo class changes the styles.

范例
transition: position, transform, background-color;
ui-scale-y
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
visibility
控制面板是否可见并包含在面板布局中。

属性值
值   描述
visible 默认，可见
collapse    不可见并且不包含在面板布局中
范例
visibility: collapse;

z
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
z-index
设置面板的z-index，面板将在父级面板中按顺序排序和绘制。排序首先根据位置计算的z-pos进行排序，然后转换，如果面板有匹配的zpos，则使用zindex。z-index与z-pos的不同之处在于，它不影响渲染透视图，只影响绘制/命中测试顺序。默认的z-index值是0，并且可以接受任何浮点值。

范例
z-index: 1;

z-index
设置面板的z-index，面板将在父级面板中按顺序排序和绘制。排序首先根据位置计算的z-pos进行排序，然后转换，如果面板有匹配的zpos，则使用zindex。z-index与z-pos的不同之处在于，它不影响渲染透视图，只影响绘制/命中测试顺序。默认的z-index值是0，并且可以接受任何浮点值。

范例
z-index: 1;
background-size
Sets the horizontal and vertical dimensions used to draw the background image. Can be set in pixels, percent, "contains" to size down to panel dimensions or "auto" preserves the image aspect ratio. By default, set to "auto" which preveres the image's original size. Multiple background layers can be specified in a comma separated list, which are then combined with background-image, background-position, background-texture-size and background-repeat values.

范例
background-size: auto; // same as "auto auto" (default) 
background-size: 100% 100%; // image fills the panel
background-size: 50% 75%; // image fills 50% of the panel's width, and 75% of the panel's height
background-size: 300px 200px; // image is drawn 300px wide, 200px tall
background-texture-size
Sets the size used for generating textures from vector graphics (.svg files), default is -1 which takes the size from the svg file.

范例
background-texture-size: 100px 50px; // width 100px, height 50px
font-size
以像素为单位指定目标字体大小。px可以省略。

属性值
值   描述
<pixel> 任意固定像素值
范例
font-size: 12;
font-size: 12px;
horizontal-align
水平方向上的定位

属性值
值   描述
left    默认，左
right   右
center  水平居中
middle  等同于center
范例
horizontal-align: right;
ui-scale-z
Specifies a scale to apply to this panel's layout and all descendants. This scale happens at the layout level ratherthan the bitmap level, so things like text will increase their font size rather than just bitmap scaling.

范例
ui-scale: 150%; // 150% scaling for X, Y, and Z.
ui-scale: 50% 100% 150%; // 50% scaling for X, 100% for Y. 150% for Z.
z
Sets the x, y, z position for a panel. Must not be in a flowing layout.

范例
position: 3% 20px 0px;